# Required at build time. This exact HTTPS origin is embedded in physical QR
# codes, so production builds intentionally fail when it is absent.
VITE_PUBLIC_SITE_URL=https://your-confirmed-public-domain.example

