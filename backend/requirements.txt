# ── Core ──────────────────────────────────────────────────────────────────────
# مراجعة Codex 2026-08-31 (SEC-05): fastapi/starlette اترقّوا معًا (starlette
# محدود بقيد fastapi نفسه) — starlette==0.41.3 القديمة كانت فيها 9 CVE
# (تفصيل كامل في docs/agent-workflow/handoffs/2026-08-31_SEC-fixes_claude_
# handoff.md) — أخطرهم Range-header ReDoS في FileResponse (DoS غير مصادَق).
# باقي الـCVEs (Host-header URL confusion، HTTPEndpoint method dispatch،
# Windows UNC SSRF) غير مستغَلّة فعليًا هنا (المشروع بيعتمد JWT+role
# checks مش request.url، بيستخدم FastAPI routers مش raw HTTPEndpoint،
# وبينشر على Linux مش Windows) — لكن التحديث بيقفلهم كلهم برضو.
fastapi==0.122.0
starlette==0.50.0
uvicorn[standard]==0.32.1
python-multipart==0.0.32

# ── Database ──────────────────────────────────────────────────────────────────
sqlalchemy==2.0.36
alembic==1.14.0
psycopg[binary]==3.2.3

# ── Settings & Validation ─────────────────────────────────────────────────────
pydantic==2.10.4
pydantic-settings==2.6.1

# ── Auth ──────────────────────────────────────────────────────────────────────
# مراجعة Codex 2026-08-31 (SEC-04): python-jose==3.3.0 عندها CVE-2024-33663
# حرجة (algorithm confusion في تحقق JWT) — نفس المكتبة المستخدمة لكل طلب
# مصادَق في auth chain المشروع كله. 3.5.0 هي أحدث نسخة مستقرة (الإصلاح
# نفسه موجود من 3.4.0).
python-jose[cryptography]==3.5.0
passlib[bcrypt]==1.7.4
bcrypt==5.0.0
pyotp==2.10.0

# ── Cache & Queue ─────────────────────────────────────────────────────────────
redis==5.2.1
celery==5.4.0
celery[redis]==5.4.0

# ── Logging & Monitoring ──────────────────────────────────────────────────────
loguru==0.7.3
sentry-sdk[fastapi]==2.19.2

# ── Encryption ────────────────────────────────────────────────────────────────
# مراجعة Codex 2026-08-31 (SEC-05): 8 CVE على 44.0.0 (pip-audit) — أحدث
# نسخة مستقرة بتقفلهم كلهم. مستخدمة هنا أساسًا لـFernet (تشفير PII)، مش
# مسارات X.509/PKCS#7 اللي كانت أغلب الثغرات فيها.
cryptography==50.0.1

# ── HTTP & Utils ──────────────────────────────────────────────────────────────
httpx==0.28.1
python-dateutil==2.9.0
openpyxl==3.1.5

# ── Notifications ─────────────────────────────────────────────────────────────
twilio==9.10.9
sendgrid==6.12.4

# ── Reports (PDF + Excel) ───────────────────────────────────────────────────
reportlab==5.0.0
arabic-reshaper==3.0.1
python-bidi==0.6.10
qrcode[pil]==8.2

# ── Dev / Testing ─────────────────────────────────────────────────────────────
pytest==8.3.4
pytest-asyncio==0.24.0
pytest-cov==6.0.0
fakeredis==2.26.2
