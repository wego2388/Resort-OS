# لوحة محمد الحالية — El Kheima Beach Resort OS

**آخر تحديث:** 29 يوليو 2026 بعد النشر
**المالك:** Mohamed
**قائد التنفيذ والمراجع النهائي:** Codex

هذه هي لوحة المالك الحالية فقط. التاريخ التفصيلي القديم محفوظ تحت
`docs/archive/2026-07-execution/` ولا تُنفّذ منه تعليمات.

## الخلاصة

الإصدار `ac7764f` منشور الآن على الـVPS من مجلد ثابت:
`/opt/resort-os-releases/ac7764f`. الخلفية وCelery وواجهة الخيمة وNginx
تعمل بالصور الجديدة، وكل health checks ناجحة، ولا توجد restarts أو أخطاء
حرجة بعد النشر.

| البند | الحالة الحالية |
|---|---|
| تطبيق الموظفين | `https://191.218.161.133` — HTTP 200 |
| الموقع التسويقي | `https://191.218.161.133:8443` — HTTP 200 |
| نموذج التشغيل | فرع تشغيلي واحد؛ لا Branch Switcher ظاهر |
| قاعدة البيانات | Alembic `88d1c505a9dc`؛ سليمة ومتاحة |
| الحاويات | 8/8 Running؛ كل الخدمات ذات healthcheck سليمة |
| النسخ الاحتياطي | نسخة DB مشفرة خارج الخادم + restore كامل ناجح |
| TLS | شهادة IP صحيحة؛ dry-run للتجديد ناجح والمؤقت يعمل |
| المراقبة | health gate آلي كل 5 دقائق؛ 14 فحصًا ناجحًا |
| DNS/domain | متوقف بقرار المالك؛ التشغيل IP-only |

## ما أُغلق في هذه الجولة

- مراجعة تعديلات المصادقة والصلاحيات وعزل الفرع وربط Offline Queue بالهوية.
- نجاح full backend بصفر failure، و63 اختبارًا مركزًا، و93/93 frontend،
  وtype-check وproduction build و`agent-check`.
- حفظ وإعادة تركيب مصدر الإنتاج القديم غير النظيف byte-for-byte قبل لمسه.
- دفع فرع العمل الوحيد إلى GitHub حتى `ac7764f` مع بقاء `main` بلا تغيير.
- إنشاء نقطة تراجع للصور ونسخة DB جديدة قبل النشر.
- نشر متدرج من release immutable بدل البناء فوق `/opt/resort-os` القديم.
- تحقق خارجي وداخلي من HTTPS وDB وRedis وCelery والشهادة والسجلات.
- تنظيف ملفات التحكم الحالية ونقل السجلات القديمة إلى الأرشيف.

## ما لم يُعتمد تشغيليًا بعد

النشر التقني ناجح، لكن قرار **Go/No-Go التشغيلي النهائي** ينتظر:

1. UAT فعلي على هاتف وجهاز موظف، بالعربية والإنجليزية، مع أدوار حقيقية.
2. اعتماد بيانات الغرف والأسعار والمخزون ودليل التشغيل والمالية بدل أي
   افتراض أو بيانات تجريبية.
3. تجربة QR وguest alerts والطباعة وانقطاع الشبكة وNight Audit وPOS ميدانيًا.
4. اختيار قناة خارجية لإرسال فشل المراقبة؛ الفحص المحلي والـjournal يعملان.
5. يظل provider snapshot تحسينًا مطلوبًا؛ النسخة المشفرة خارج الخادم موجودة
   بالفعل كحد أدنى قابل للاستعادة.

لا يُفعّل Chatbot أو يُغيّر DNS أو تُزرع بيانات demo دون قرار صريح جديد من
Mohamed. ملف `scripts/wait-dns-then-switch.sh` مملوك للمستخدم ولم يُعدّل أو
يُشغّل.

## أين نتابع

- الحقيقة التقنية: `PROJECT_STATUS.md`
- الخطة الحية: `docs/audits/EL_KHEIMA_FINAL_EXECUTION_PLAN_AR.md`
- المهمة الحالية: `docs/agent-workflow/EL_KHEIMA_EXECUTION_BOARD.md`
- تسليم النشر: `docs/agent-workflow/handoffs/2026-07-29_REL-02_codex_handoff.md`
